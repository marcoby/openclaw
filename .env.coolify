# OpenClaw Coolify Environment Variables
# Copy this file and set the values in Coolify's Environment Variables UI
# or create a .env file (for local testing)

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# Gateway authentication token (64+ characters recommended)
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# Setup wizard password (protects /setup endpoint)
SETUP_PASSWORD=

# =============================================================================
# HTTP SERVER CONFIGURATION
# =============================================================================

# Port for the HTTP server (Coolify default is 8080)
PORT=8080

# Gateway port (should match PORT)
OPENCLAW_GATEWAY_PORT=8080

# Gateway bind address (lan = listen on all interfaces)
OPENCLAW_GATEWAY_BIND=lan

# Bridge port (internal, usually doesn't need to be exposed)
OPENCLAW_BRIDGE_PORT=18790

# =============================================================================
# PERSISTENT STORAGE PATHS
# =============================================================================

# Directory for OpenClaw state, config, and credentials
OPENCLAW_STATE_DIR=/data/.openclaw

# Directory for agent workspace files
OPENCLAW_WORKSPACE_DIR=/data/workspace

# =============================================================================
# PROVIDER CREDENTIALS (OPTIONAL)
# =============================================================================
# You can set these here OR configure them via the /setup wizard
# The wizard is recommended for easier setup

# Claude AI session key (from claude.ai)
CLAUDE_AI_SESSION_KEY=

# Microsoft Teams / Azure Bot (SharePoint Integration)
MSTEAMS_APP_ID=
MSTEAMS_APP_PASSWORD=
MSTEAMS_TENANT_ID=

# Claude web session key (alternative auth method)
CLAUDE_WEB_SESSION_KEY=

# Claude web cookie (alternative auth method)
CLAUDE_WEB_COOKIE=

# =============================================================================
# MESSAGING CHANNELS (OPTIONAL)
# =============================================================================
# Configure channels via /setup wizard or CLI after deployment

# Telegram bot token (from @BotFather)
TELEGRAM_BOT_TOKEN=

# Discord bot token (from Discord Developer Portal)
# DISCORD_BOT_TOKEN=

# Slack bot token (from Slack App settings)
# SLACK_BOT_TOKEN=

# Microsoft Teams (Azure Bot)
# Requires registering an Azure Bot (see docs/channels/msteams.md)
MSTEAMS_APP_ID=
MSTEAMS_APP_PASSWORD=
MSTEAMS_TENANT_ID=

# =============================================================================
# ADVANCED OPTIONS (USUALLY NOT NEEDED)
# =============================================================================

# Node environment
NODE_ENV=production

# Docker image name (for docker-compose builds)
OPENCLAW_IMAGE=openclaw:latest

# Additional APT packages to install during Docker build
OPENCLAW_DOCKER_APT_PACKAGES=

# Extra volume mounts (comma-separated)
OPENCLAW_EXTRA_MOUNTS=

# Home volume name (for advanced volume management)
OPENCLAW_HOME_VOLUME=
